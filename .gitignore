# --- Secrets: never commit ---
.env
.env.*
!.env.example

# --- Node ---
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# --- Build / output ---
dist/
build/
coverage/
*.log

# --- OS / editor ---
.DS_Store
Thumbs.db
.vscode/
.idea/

# --- Scratch / local test output ---
tmp/
out/

# --- Live mailbox ingestion: local processed-state + pulled emails (contain PII) ---
src/ingestion/.ingest-state.json
.inbox/

# --- Real customer sample data: DO NOT publish (regenerable from the mailbox) ---
# Contains customer names, contacts, PO numbers, pricing. Keep out of the (public) repo.
# The generic schema and README below are intentionally NOT ignored.
samples/raw/
samples/answer-keys/
samples/manifest.csv
samples/FINDINGS.md
samples/*-report.json
